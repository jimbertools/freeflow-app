name: app
description: Mobile App Isolation

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_inappwebview: ^5.3.2
  cupertino_icons: ^1.0.2
  image_picker: 0.8.4+1
  geolocator: 8.0.0
  sensors: 2.0.3
  local_auth: ^1.1.6
  qr_code_scanner: 0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true